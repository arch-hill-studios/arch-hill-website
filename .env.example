# =============================================================================
# NEXT.JS + SANITY TEMPLATE - ENVIRONMENT VARIABLES
# =============================================================================
# Copy this file to .env.local and fill in your values:
#   cp .env.example .env.local
#
# IMPORTANT: Never commit .env.local to version control!
# =============================================================================

# =============================================================================
# ENVIRONMENT MODE
# =============================================================================
# Set to "production" for live site, anything else for development
# This affects: robots.txt (noindex in non-production), revalidation behavior
NEXT_PUBLIC_ENV="development"

# =============================================================================
# SANITY CMS CONFIGURATION
# =============================================================================
# Get these values from https://www.sanity.io/manage
# 1. Create a new project (or use existing)
# 2. Copy the Project ID from the project dashboard

# Your Sanity Project ID (found in Sanity project settings)
NEXT_PUBLIC_SANITY_PROJECT_ID="your-sanity-project-id"

# Dataset name - typically "production" for live site, "development" for testing
NEXT_PUBLIC_SANITY_DATASET="development"

# Sanity API Read Token (required for draft mode and live preview)
# Create in Sanity: Settings > API > Tokens > Add API Token
# Permissions: "Viewer" is sufficient for read operations
SANITY_API_READ_TOKEN="your-sanity-read-token"

# Sanity API Write Token (optional - only needed for migration scripts)
# Create in Sanity: Settings > API > Tokens > Add API Token
# Permissions: "Editor" required for write operations
SANITY_API_WRITE_TOKEN="your-sanity-write-token"

# Sanity Webhook Secret (for on-demand revalidation)
# Generate a random string (e.g., using: openssl rand -hex 32)
# Use the same secret when configuring the webhook in Sanity dashboard
# Webhook URL: https://your-domain.com/api/revalidate
SANITY_WEBHOOK_SECRET="your-webhook-secret"

# =============================================================================
# SITE CONFIGURATION
# =============================================================================
# Base URL for the site (used for SEO, canonical URLs, sitemaps, etc.)
# Development: http://localhost:3000
# Production: Your actual domain (e.g., https://yoursite.com)
# IMPORTANT: No trailing slash!
NEXT_PUBLIC_BASE_URL="http://localhost:3000"

# =============================================================================
# EMAIL CONFIGURATION (Resend)
# =============================================================================
# This template uses Resend for transactional emails (contact forms, etc.)
# Get your API key from https://resend.com

# Resend API Key
RESEND_API_KEY="your-resend-api-key"

# Email address where form submissions should be sent
NEXT_PUBLIC_CONTACT_EMAIL="contact@yoursite.com"

# From email for automated messages
# Development: Use "onboarding@resend.dev" (Resend's test domain)
# Production: Use your verified domain (e.g., "noreply@yoursite.com")
# Format: "Display Name <email@domain.com>" or just "email@domain.com"
RESEND_FROM_EMAIL="Your Site <onboarding@resend.dev>"
